function toArray(data) {
  if(data.all) {
    return data.all().filter(el=>(!['Custom','CAP'].includes(el.isNonstandard)));
  }
  const results = [];
  for(const i of data){
    results.push(i);
  }
  return results;
}

module.exports = toArray;
